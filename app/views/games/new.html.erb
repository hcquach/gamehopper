<%= content_for(:title) do %>
Game Hopper-Create Game
<% end %>
<div class="background-image">
  <div class="container">
    <h2>Post a game</h2>
    <div class="row">
      <div class="col-sm-12 col-md-4 col-md-offset-4">
        <%= simple_form_for(@game, html: {class: 'form-horizontal' }) do |f| %>
          <%= f.input :title, class: "title-input" %>
          <%= f.input :description, :as => :text, :input_html => { :rows => 7 } %>
          <%= f.input :price %>
          <%= f.input :platform, collection: Game::PLATFORMS %>
          <%= f.input :photo %>
          <%= f.input :photo_cache, as: :hidden %>
          <div class="create-btn">
          <%= f.submit "Create", class: "btn btn-primary btn-lg btn-block" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
